Run the script and then run the output command in the file, in the terminal.
Ensure that when you run the output command, you are in the same directory as the student files.

ADDITIONAL INFORMATION:
you may have to install whatever random packages the students have used in their code.